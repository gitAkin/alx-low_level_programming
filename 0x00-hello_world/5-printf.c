#include <stdio.h>
/**
 * main- this is another C programme that prints a specific output using printf
 *
 * Return: (0) if no error(s) and return non (0) if error(s)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
