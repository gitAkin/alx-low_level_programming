#include <stdio.h>
/**
 *main- this is a c programe that prints exactly the required output
 *
 *Return: By default returns 0 if no errors otherwise returns non 0 value.
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
